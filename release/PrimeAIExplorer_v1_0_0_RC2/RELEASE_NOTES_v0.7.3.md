# PrimeAIExplorer v0.7.3

## Legacy report compatibility

- Safely renders missing/null legacy metrics in Markdown and HTML comparative reports.
- Displays an em dash for unavailable values instead of coercing null to zero.
- Preserves ranking behavior that ignores unavailable metrics.
- Adds regression coverage for null mean error, signed error, and switch rate.
