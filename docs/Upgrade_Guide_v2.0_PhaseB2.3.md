# Upgrade Guide — v2.0 Phase B2.3

Required version:

```text
2.0.0-phase-b2.2-r3
```

Installed version:

```text
2.0.0-phase-b2.3
```

B2.3 preserves all B2.1 and B2.2 provider types and adds:

```json
"provider_type": "partitioned_gap_uint16"
```
