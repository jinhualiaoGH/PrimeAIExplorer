# PrimeAIExplorer v1.2.2 Fixture Correction

## Correct mathematical count

The synthetic gap array is:

```text
1, 2, 2, 4, 2, 4, 2, 4, 6, 2, 10, 2
```

The value `2` occurs at six positions:

```text
1, 2, 4, 6, 9, 11
```

Applying the canonical left-owned rule selects these primes:

```text
3, 5, 11, 17, 29, 41
```

Therefore:

```text
left_twin_count = 6
```

The v1.2.1 implementation correctly returned six. Only the test expectation was
wrong.

## Additional consistency fix

The sequence-plugin registry and the adapter now both report:

```text
left_twin version = 1.2.2
```

## Installer behavior

The explicit `$LASTEXITCODE` checks introduced in v1.2.1 are retained.
