# Upgrade Guide — v2.0 Phase B1.3

Required baseline:

```text
2.0.0-phase-b1.2
```

Installed version:

```text
2.0.0-phase-b1.3
```

This release is additive. It does not modify the B1.1 `kernel` package or B1.2
`runtime` package.
