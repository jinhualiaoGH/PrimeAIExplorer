# Upgrade Guide — v2.0 Phase B2.1

Required baseline:

```text
2.0.0-phase-b1.4
```

Installed version:

```text
2.0.0-phase-b2.1
```

B2.1 is additive and preserves the B1.1–B1.4 APIs. It introduces the
`sequence_api` package and does not modify the mathematical behavior of any
existing plugin.
