# Prime Value Dataset Engine — Phase B

Adds atomic `.npy` construction, metadata, SHA-256, overwrite protection, chunked monotonic validation, and deterministic primality sampling.

The production dataset contains 100,000,001 uint64 values (about 800 MB of array data).
