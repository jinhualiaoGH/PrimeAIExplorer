# Upgrade Guide — v2.0 Phase B2.6

Required version:

```text
2.0.0-phase-b2.5
```

Installed version:

```text
2.0.0-phase-b2.6
```

B2.6 preserves all B2.5 sequence and dataset operations. It adds deterministic
prompt templates, prompt generation, prompt batches, response contracts, and
optional ground-truth exposure for controlled internal auditing.
