# Upgrade Guide — v2.0 Phase B2.5

Required version:

```text
2.0.0-phase-b2.4
```

Installed version:

```text
2.0.0-phase-b2.5
```

B2.5 preserves all existing sequence providers and operations. It adds a
dataset registry and deterministic dataset operations to the existing
`sequence_api` plugin.
