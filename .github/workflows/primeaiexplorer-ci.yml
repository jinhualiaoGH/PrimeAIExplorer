name: PrimeAIExplorer CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: py -m pip install -r requirements.txt
      - run: py -m compileall -q .
      - run: py -m unittest discover -s tests -v
